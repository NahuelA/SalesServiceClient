import { ReloadSalesDirective } from './reload-sales.directive';

describe('ReloadSalesDirective', () => {
  it('should create an instance', () => {
    const directive = new ReloadSalesDirective();
    expect(directive).toBeTruthy();
  });
});
